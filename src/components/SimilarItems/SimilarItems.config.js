// ─── Similar Items configuration ──────────────────────────────────────────────
// Adjust PRICE_RANGE_PERCENT to control how wide the "similar price" band is.
// E.g. 0.4 means ±40% of the current phone's price.
export const SIMILAR_ITEMS_CONFIG = {
  PRICE_RANGE_PERCENT: 0.4, // ±40 % price band
  MAX_ITEMS: 10,            // max cards to show in the strip
}
